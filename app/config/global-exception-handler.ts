import {ErrorHandler} from "@angular/core";

export class GlobalExceptionHandler implements ErrorHandler {
  handleError(error) { //TODO: remove global error meesage in console in case of exception; Think for better solution
  }
}
